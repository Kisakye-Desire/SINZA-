import { SiteShell, EnquiryCta } from '@/components/site-shell'
const steps = ['Listen to what you want from the journey.', 'Recommend the right country, season and rhythm.', 'Shape a route with stays and experiences that fit.', 'Refine every detail together before you travel.', 'Stay close while you are away and after you return.']
export default function HowWeWorkPage() { return <SiteShell><main><section className="intel-article-hero"><p className="eyebrow">Our approach</p><h1>How we work</h1><p>Thoughtful planning, local expertise and a route that never feels like somebody else&apos;s.</p></section><section className="work-steps">{steps.map((step, index) => <article key={step}><span>0{index + 1}</span><h2>{step}</h2></article>)}</section><EnquiryCta /></main></SiteShell> }
