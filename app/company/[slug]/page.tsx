import { notFound } from 'next/navigation'
import { PageHero, SiteShell } from '@/components/site-shell'
const pages: Record<string, { title: string; intro: string; body: string[] }> = { 'our-story': { title: 'Our Story', intro: 'From a love of home to a way of sharing it.', body: ['Sinza Safaris is an independent, Uganda-based safari company. We design private journeys for travellers who want to go beyond the obvious and feel a genuine connection to place.', 'Our team brings together field knowledge, thoughtful logistics and a deep respect for the landscapes and communities that make East Africa so powerful.'] }, conservation: { title: 'Conservation', intro: 'Wild places are not scenery. They are living systems.', body: ['We support travel that values the people and habitats at its heart. That means choosing responsible partners, respecting protected areas and making space for community-led experiences.', 'We are always learning. Conservation is not a single claim; it is a daily practice of better decisions, honest conversations and long-term relationships.'] }, team: { title: 'The Team', intro: 'The people who make the details feel effortless.', body: ['From the first call to the final airport transfer, our team stays close to the journey. We know the roads, the seasons, the guides and the small places worth slowing down for.', 'You will have a dedicated journey designer before departure and a trusted local network around you while you travel.'] } }
export default async function CompanyDetailPage({ params }: { params: Promise<{ slug: string }> }) { const { slug } = await params; const page = pages[slug]; if (!page) notFound(); return <SiteShell><PageHero eyebrow="Company" title={<>{page.title.split(' ')[0]}<br /><em>{page.title.split(' ').slice(1).join(' ')}</em></>} intro={page.intro} image="https://images.unsplash.com/photo-1500534623283-312aade485b7?auto=format&fit=crop&w=1400&q=85" /><article className="article-body">{page.body.map((paragraph) => <p key={paragraph}>{paragraph}</p>)}</article></SiteShell> }
