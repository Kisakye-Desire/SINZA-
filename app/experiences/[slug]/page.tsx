import { notFound } from 'next/navigation'
import { experiences } from '@/lib/safari-data'
import { SiteShell, EnquiryCta } from '@/components/site-shell'
export function generateStaticParams() { return experiences.map((item) => ({ slug: item.slug })) }
export default async function ExperienceDetail({ params }: { params: Promise<{ slug: string }> }) { const { slug } = await params; const item = experiences.find((entry) => entry.slug === slug); if (!item) notFound(); return <SiteShell><main><section className="detail-hero"><img src={item.image} alt={item.title} /><div><p className="eyebrow">Experience guide</p><h1>{item.title}</h1><p>{item.description}</p></div></section><section className="section-wrap detail-layout"><article><p className="eyebrow">Go deeper</p><h2>A feeling<br /><em>to carry home.</em></h2><p className="large-copy">We build the days around this experience with room for the unexpected: the right guide, a thoughtful pace and the small local details that transform an activity into a story.</p></article><aside className="detail-aside"><p className="eyebrow">What to expect</p><p className="season-copy">Local guides, respectful encounters, flexible pacing and an itinerary designed around your interests.</p></aside></section><EnquiryCta /></main></SiteShell> }
