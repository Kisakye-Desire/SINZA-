import Link from 'next/link'
import { ArrowRight } from 'lucide-react'
import { SiteShell, EnquiryCta } from '@/components/site-shell'
import { destinations, destinationDetails } from '@/lib/safari-data'

export default function DestinationsPage() { return <SiteShell><main><section className="destination-intro"><p className="eyebrow">Destinations</p><h1>Where do you<br /><em>want to go?</em></h1><p>Uganda and Rwanda. Countless ways to feel the wild.</p></section><section className="destination-country-list"><Link className="destination-country-row destination-all" href="/safaris"><span className="country-number">—</span><span className="country-name">All safaris</span><span className="country-tagline">See every journey in one place</span><ArrowRight size={18} /></Link>{destinationDetails.map((country, index) => <Link className="destination-country-row" href={`/destinations/${country.slug}`} key={country.slug}><span className="country-number">0{index + 1}</span><span className="country-name">{country.name}</span><span className="country-tagline">{country.tagline}</span><ArrowRight size={18} /></Link>)}</section><section className="section-wrap destination-card-section"><div className="section-heading"><div><p className="eyebrow">Plan by landscape</p><h2>Find your<br /><em>way in.</em></h2></div><p>From primate-rich forests to river and savannah country, each destination offers a different rhythm for seeing Uganda and Rwanda.</p></div><div className="destination-card-grid">{destinations.map((destination, index) => <Link className="destination-card" href={`/destinations/${destination.slug}`} key={destination.slug}><div className="destination-card-image"><img src={destination.image} alt={destination.name} /><span>0{index + 1}</span></div><div className="destination-card-copy"><p className="eyebrow">{destination.region}</p><h3>{destination.name}</h3><p>{destination.description}</p><span className="card-link">Explore destination <ArrowRight size={15} /></span></div></Link>)}</div></section><section className="destination-feature"><div><p className="eyebrow">Start with a conversation</p><h2>Find your<br /><em>way in.</em></h2><p>Tell us what brings you to Africa and we will point you toward the right landscape and route.</p><Link href="/contact" className="text-link">Plan around you <ArrowRight size={17} /></Link></div><img src="/images/wildlife/hippo-water.jpg" alt="Hippos in water in Uganda" /></section><EnquiryCta /></main></SiteShell> }
