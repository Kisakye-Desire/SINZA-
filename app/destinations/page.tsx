import Link from 'next/link'
import { ArrowRight } from 'lucide-react'
import { SiteShell, EnquiryCta } from '@/components/site-shell'
import { destinationDetails } from '@/lib/safari-data'

export default function DestinationsPage() { return <SiteShell><main><section className="destination-intro"><p className="eyebrow">Destinations</p><h1>Where do you<br /><em>want to go?</em></h1><p>Four countries. Countless ways to feel the wild.</p></section><section className="destination-country-list"><Link className="destination-country-row destination-all" href="/safaris"><span className="country-number">—</span><span className="country-name">All safaris</span><span className="country-tagline">See every journey in one place</span><ArrowRight size={18} /></Link>{destinationDetails.map((country, index) => <Link className="destination-country-row" href={`/destinations/${country.slug}`} key={country.slug}><span className="country-number">0{index + 1}</span><span className="country-name">{country.name}</span><span className="country-tagline">{country.tagline}</span><ArrowRight size={18} /></Link>)}</section><section className="destination-feature"><div><p className="eyebrow">Start with a conversation</p><h2>Find your<br /><em>way in.</em></h2><p>Tell us what brings you to Africa and we will point you toward the right landscape and route.</p><Link href="/contact" className="text-link">Plan around you <ArrowRight size={17} /></Link></div><img src="/images/wildlife/hippo-water.jpg" alt="Hippos in water in Uganda" /></section><EnquiryCta /></main></SiteShell> }
